import React from 'react';
import {documentChanges} from './review.mjs';
const display=value=>value===undefined?'(absent)':JSON.stringify(value,null,2);
export default function ReviewPanel({baseline,document,writable,onSave}){const changes=documentChanges(baseline,document);const groups=Map.groupBy(changes,c=>c.path.slice(0,2).join(' / ')||'Document');return <div className="properties"><h2>JSON change review</h2><p className="muted">Compared with the last opened or saved version. Reviewing does not write the source file.</p><p>{changes.length} field changes · {changes.filter(c=>c.category==='layout').length} layout · {changes.filter(c=>c.category==='topology').length} topology · {changes.filter(c=>c.category==='content').length} content</p><div className="review-actions">{writable&&<button onClick={()=>onSave(true)}>Save reviewed file</button>}<button onClick={()=>onSave(false)}>Download reviewed JSON</button></div>{[...groups].map(([group,entries])=><details key={group} open><summary>{group}</summary>{entries.map((change,index)=><section className="change-entry" key={index}><strong>{change.path.slice(2).join('.')||'Item'} · {change.category}</strong><span>Before</span><pre>{display(change.before)}</pre><span>After</span><pre>{display(change.after)}</pre></section>)}</details>)}{!changes.length&&<p>No changes since the baseline.</p>}</div>;}
