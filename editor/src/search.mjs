import {sourceNodes,connections} from './adapters/index.mjs';
export function searchDiagram(document,query){const term=query.trim().toLocaleLowerCase();if(!term)return [];const nodes=sourceNodes(document);return [...nodes.map(n=>({kind:'node',id:n.id,label:n.label,ids:[n.id],text:`${n.id} ${n.label} ${n.sublabel||''}`})),...connections(document).map((e,index)=>({kind:'connection',id:e.id||String(index+1),index,label:e.label||`${e.from} → ${e.to}`,ids:[e.from,e.to],text:`${e.id||''} ${e.label||''} ${e.from} ${e.to} ${nodes.find(n=>n.id===e.from)?.label||''} ${nodes.find(n=>n.id===e.to)?.label||''}`}))].filter(r=>r.text.toLocaleLowerCase().includes(term));}
